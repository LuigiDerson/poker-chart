import React from 'react'

export default class UserOptions extends React.Component {
  render() {
    return (
      <div>
        <button>Fold</button>
        <button>Call</button>
        <button>Raise</button>
      </div>
    )
  }
}
